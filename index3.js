const strs = ["flower", "flow", "flight"];
commonPrefixCheck(strs);

function commonPrefixCheck(str) {
  let newStr = "";
  for (let i = 0; i < str.length; i++) {
    for (let j = 0; j < str[i].length; j++) {
      if (str[i][j]) {
        
      }
    }
  }
}
